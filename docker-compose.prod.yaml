version: "3.0"
services:
  client:
    image: client-prod-i
    build:
      dockerfile: Dockerfile.prod
    container_name: client-prod-c
    ports:
      - "9012:9012"
    environment:
      - NODE_ENV=production